# VGP232 Assignment 1 – CSV Parser

## Build & Run

Open `Assignment1.sln`

Set Command Line Arguments:
-i data.csv -o output.csv -c -s BaseAttack

Press Run ▶

## Output

Generates `output.csv` inside bin folder sorted by BaseAttack and prints entry count.
